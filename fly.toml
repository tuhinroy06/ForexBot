app = "forex-signal-bot"
primary_region = "sin"

[build]

[[services]]
  internal_port = 8080
  protocol = "tcp"
  auto_start_machines = true
  auto_stop_machines = false
  min_machines_running = 1

[env]
  PORT = "8080"
